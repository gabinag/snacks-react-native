import{StyleSheet} from 'react-native'


const styles = StyleSheet.create({
  imagem: {
    width: 200, 
    height: 200, 
    margin: 'auto',
    marginBottom: 20,
    borderRadius: 100
  }
});

export {styles}